import{_ as o,c as t,b as n,o as r}from"./app-B8SEAMDd.js";const s="/blog/images/fiberworking1.png",a="/blog/images/fiberworking2.png",c="/blog/images/fiberworking3.png",i="/blog/images/fiberworking4.png",d="/blog/images/fiberworking5.png",p={};function l(h,e){return r(),t("div",null,[...e[0]||(e[0]=[n(`<h2 id="foreword" tabindex="-1"><a class="header-anchor" href="#foreword"><span>Foreword</span></a></h2><p>We have talked about what excatly <code>Fiber</code> is and we know that <code>Fiber Nodes</code> could save its corresponding <code>DOM Node</code>.</p><p>Aslo, <code>Fiber Tree</code> built by <code>Fiber Nodes</code> corresponding the <code>DOM Tree</code>.</p><p>In this section, we will talking about how to use <code>Double Buffer</code> for update <code>DOM</code>.</p><h2 id="double-buffer" tabindex="-1"><a class="header-anchor" href="#double-buffer"><span>Double Buffer</span></a></h2><p>When we draw animation with <code>canvas</code>, <code>ctx.clearRect</code> will be called before each frame is drawn to clear the previous frame.</p><p>If the current frame includes heavy computation, the <code>white screen</code> will appear during the long gap between clearing the previous frame and drawing the current frame.</p><p>To solve this problem, we can draw the current frame in the memory and replace the previous frame with the current frame directly after drawing, which will not cause the white screen because the computation time between those two replacement frames is eliminated.</p><p>This in-memory build and direct replacement technique is called <code>double buffer</code>.</p><p><code>React</code> uses <code>double buffer</code> to build and replace the <code>Fiber tree</code> - corresponding to the creation and update of the <code>DOM tree</code>.</p><h2 id="double-buffer-fiber-tree" tabindex="-1"><a class="header-anchor" href="#double-buffer-fiber-tree"><span>Double Buffer Fiber Tree</span></a></h2><p>There will be at most two <code>Fiber tree</code> in <code>React</code> at the same time. The <code>Fiber tree</code> corresponding to the content currently displayed on the screen is called the <code>current tree</code>, and the <code>Fiber tree</code> which built in the memory is called the <code>workInProgress tree</code>.</p><p>The <code>Fiber node</code> in the <code>current Fiber tree</code> are called <code>current fiber</code>, and the <code>Fiber node</code> in the <code>workInProgress Fiber tree</code> are called <code>workInProgress fiber</code>, and they are connected by the <code>alternate</code> property.</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line">currentFiber<span class="token punctuation">.</span>alternate <span class="token operator">===</span> workInProgressFiber</span>
<span class="line">workInProgressFiber<span class="token punctuation">.</span>alternate <span class="token operator">===</span> currentFiber</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The root node of the <code>React</code> application switches the <code>current tree</code> pointers by making the <code>current</code> pointer switch between the different trees <code>rootFiber</code>.</p><p>That is, when the <code>workInProgress tree</code> is built and rendered on the page by the <code>Renderer</code>, the <code>current</code> pointer of the root node of the application points to the <code>workInProgress tree</code>, and the <code>workInProgress tree</code> becomes the <code>current tree</code>.</p><p>Every time the state is updated, a new <code>workInProgress tree</code> will be generated and the <code>DOM</code> update will be completed by replacing <code>current</code> with <code>workInProgress</code>.</p><p>Next, we will explain the build/replace process when <code>mount</code> and <code>update</code> with concrete examples.</p><h2 id="mount" tabindex="-1"><a class="header-anchor" href="#mount"><span>mount</span></a></h2><p>Take a look at this example first</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line"><span class="token keyword">function</span> <span class="token function">App</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token keyword">const</span> <span class="token punctuation">[</span>num<span class="token punctuation">,</span> add<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token function">useState</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">return</span> <span class="token operator">&lt;</span>p onClick<span class="token operator">=</span><span class="token punctuation">{</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token function">add</span><span class="token punctuation">(</span>num <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token operator">&gt;</span><span class="token punctuation">{</span>num<span class="token punctuation">}</span><span class="token operator">&lt;</span><span class="token operator">/</span>p<span class="token operator">&gt;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">ReactDOM<span class="token punctuation">.</span><span class="token function">render</span><span class="token punctuation">(</span><span class="token operator">&lt;</span>App <span class="token operator">/</span><span class="token operator">&gt;</span><span class="token punctuation">,</span> document<span class="token punctuation">.</span><span class="token function">getElementById</span><span class="token punctuation">(</span><span class="token string">&#39;root&#39;</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ol><li>The first execution of <code>ReactDOM.render</code> creates <code>fiberRootNode</code> (called <code>fiberRoot</code> in the source code) and <code>rootFiber</code>. Where <code>fiberRootNode</code> is the root node of the entire application and <code>rootFiber</code> is the root node of the component tree where <code>&lt;App/&gt;</code> is located.</li></ol><p>The reason for the distinction between <code>fiberRootNode</code> and <code>rootFiber</code> is that we can call <code>ReactDOM.render</code> multiple times in the application to render different component trees, and they will have different <code>rootFiber</code>. But there is only one root node for the whole application, and that is <code>fiberRootNode</code>.</p><p>The <code>current</code> of the <code>fiberRootNode</code> will point to the <code>Fiber tree</code> corresponding to the rendered content on the current page, i.e. the <code>current tree</code>.</p><p><img src="`+s+`" alt="fiberRootNode current point to rootFiber "></p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line">fiberRootNode<span class="token punctuation">.</span>current <span class="token operator">=</span> rootFiber</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Since it is the first screen rendering, there is no <code>DOM</code> mounted in the page yet, so the <code>rootFiber</code> pointed by <code>fiberRootNode.current</code> does not have any <code>child node</code> (i.e. the current Fiber tree is empty).</p><ol start="2"><li>Next, we are going to the <code>render</code> phase, which creates <code>Fiber node</code> in memory according to the JSX returned by the component and connects them together to build a <code>Fiber tree</code>, called <code>workInProgress tree</code>. (The right side of the figure below is the tree built in memory, and the left side is the tree displayed on the page)</li></ol><p>When building the <code>workInProgress tree</code>, we will try to reuse the properties in the existing <code>Fiber node</code> in the <code>current tree</code>, so that only the root Fiber has a corresponding <code>current fiber</code> when the first screen is rendered.</p><p><img src="`+a+'" alt="the right side of the figure below is the tree built in memory, and the left side is the tree displayed on the page"></p><ol start="3"><li>The finished <code>workInProgress tree</code> on the right side of the figure is rendered to the page at the <code>commit</code> stage.</li></ol><p>The <code>current</code> pointer of the <code>fiberRootNode</code> points to the <code>workInProgress tree</code> so that it becomes the <code>current tree</code>.</p><p><img src="'+c+'" alt="the finished workInProgress tree on the right side of the figure is rendered to the page at the commit stage"></p><h2 id="update" tabindex="-1"><a class="header-anchor" href="#update"><span>update</span></a></h2><ol><li>Next, we click on the <code>p node</code> to trigger a state change, which opens a new <code>render</code> phase and builds a new <code>workInProgress tree</code>.</li></ol><p><img src="'+i+'" alt="opens a new render phase and builds a new workInProgress tree"></p><p>As the same as <code>mount</code>, the <code>workInProgress fiber</code> can be created by reusing the node data corresponding to the <code>current tree</code>.</p><div class="custom-container tip"><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" viewBox="0 0 24 24"><g fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"></circle><path d="M12 8h.01"></path><path d="M11 12h1v4h1"></path></g></svg><p class="custom-container-title">TIP</p><p>This process of deciding whether or not to reuse is the <code>Diff</code> algorithm, which will be explained in detail in later chapter</p></div><ol start="2"><li>The <code>workInProgress tree</code> is built in the <code>render</code> phase and then rendered to the page in the <code>commit</code> phase. When rendering is complete, the <code>workInProgress tree</code> becomes the <code>current tree</code>.</li></ol><p><img src="'+d+'" alt="workInProgress tree become current tree"></p><h2 id="summary" tabindex="-1"><a class="header-anchor" href="#summary"><span>Summary</span></a></h2><p>We have learned about <code>React</code>&#39;s <code>Scheduler-Reconciler-Renderer</code> architecture system so far, it should be a good time to introducing a few terms from within the source code.</p><p>The phase in which <code>Reconciler</code> works is called the <code>render</code> phase. This is because the <code>render</code> method of the component is called in this phase.</p><p>The stage where the <code>Renderer</code> works is called the <code>commit</code> stage. The <code>commit</code> phase renders the information submitted in the render phase on the page, just like you run the <code>git commit</code>.</p><p>The <code>render</code> and <code>commit</code> phases are collectively referred to as <code>work</code>, i.e. React is at work. Correspondingly, if a task is being scheduled within the <code>Scheduler</code>, it is not work.</p>',45)])])}const b=o(p,[["render",l]]),m=JSON.parse('{"path":"/posts/reactSourceCode/fiber/workingPrinciple.html","title":"Fiber -- Working Principle","lang":"en-US","frontmatter":{"title":"Fiber -- Working Principle","date":"2023-02-15","categories":["React","Fiber Architecture"]},"headers":[{"level":2,"title":"Foreword","slug":"foreword","link":"#foreword","children":[]},{"level":2,"title":"Double Buffer","slug":"double-buffer","link":"#double-buffer","children":[]},{"level":2,"title":"Double Buffer Fiber Tree","slug":"double-buffer-fiber-tree","link":"#double-buffer-fiber-tree","children":[]},{"level":2,"title":"mount","slug":"mount","link":"#mount","children":[]},{"level":2,"title":"update","slug":"update","link":"#update","children":[]},{"level":2,"title":"Summary","slug":"summary","link":"#summary","children":[]}],"git":{"createdTime":1785043051000,"updatedTime":1785144585000,"contributors":[{"name":"harry","email":"hanyao@compac.co.nz","commits":2}]},"filePathRelative":"posts/reactSourceCode/fiber/workingPrinciple.md"}');export{b as comp,m as data};
