import{_ as d,c as p,b as s,a as n,d as a,e as o,r as i,o as l}from"./app-B8SEAMDd.js";const c={},r={href:"https://github.com/facebook/react/blob/1fb18e22ae66fdb1dc127347e169e73948778e5a/packages/react-reconciler/src/ReactUpdateQueue.old.js#L189",target:"_blank",rel:"noopener noreferrer"},u={href:"https://github.com/facebook/react/blob/1fb18e22ae66fdb1dc127347e169e73948778e5a/packages/react-reconciler/src/ReactUpdateQueue.new.js#L157",target:"_blank",rel:"noopener noreferrer"},h={href:"https://github.com/facebook/react/blob/1fb18e22ae66fdb1dc127347e169e73948778e5a/packages/react-reconciler/src/ReactUpdateQueue.new.js#L208",target:"_blank",rel:"noopener noreferrer"},b={href:"https://github.com/facebook/react/blob/1fb18e22ae66fdb1dc127347e169e73948778e5a/packages/react-reconciler/src/ReactUpdateQueue.new.js#L405",target:"_blank",rel:"noopener noreferrer"};function m(k,e){const t=i("ExternalLinkIcon");return l(),p("div",null,[e[30]||(e[30]=s(`<h2 id="foreword" tabindex="-1"><a class="header-anchor" href="#foreword"><span>Foreword</span></a></h2><p>We will talk about the structure and workflow of <code>Update</code> in this article.</p><blockquote><p>You can think <code>Update</code> as a <code>commit</code> in the <code>mental model</code>.</p></blockquote><h2 id="type-of-update" tabindex="-1"><a class="header-anchor" href="#type-of-update"><span>Type of Update</span></a></h2><p>We could group the <code>update</code> by the way how it trigger updates.</p><ul><li>ReactDOM.render -- HostRoot</li><li>this.setState -- ClassComponent</li><li>this.forceUpdate -- ClassComponent</li><li>useState -- FunctionComponent</li><li>useRender -- FunctionComponent</li></ul><p>We could find these three type of components could trigger the update, includings <code>HostRoot</code>, <code>ClassComponent</code> and <code>FunctionComponent</code>.</p><p>Based on the different type of the component, <code>React</code> has two <code>Update</code> structure, <code>HostRoot</code> and <code>ClassComponent</code> share the same <code>Update</code> structure and <code>FucntionComponent</code> has another <code>Update</code> structure.</p><p>Even those two <code>Update</code> have different srructure, but their workflow are similar.</p><p>We will talk about the <code>Update</code> for <code>HostRoot</code> and <code>ClassComponent</code> in this ariticle, will dicuss about the <code>Update</code> of <code>FunctionComponent</code> in the future section <code>Hooks</code>.</p><h2 id="structure-of-update" tabindex="-1"><a class="header-anchor" href="#structure-of-update"><span>Structure of Update</span></a></h2><p><code>ClassComponent</code> and <code>HostRoot</code> share the same <code>Update structure</code>.</p><p>The corresponding structure looks like</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line"><span class="token keyword">const</span> <span class="token literal-property property">update</span><span class="token operator">:</span> Update<span class="token operator">&lt;</span><span class="token operator">*</span><span class="token operator">&gt;</span> <span class="token operator">=</span> <span class="token punctuation">{</span></span>
<span class="line">  eventTime<span class="token punctuation">,</span></span>
<span class="line">  lane<span class="token punctuation">,</span></span>
<span class="line">  suspenseConfig<span class="token punctuation">,</span></span>
<span class="line">  <span class="token literal-property property">tag</span><span class="token operator">:</span> UpdateState<span class="token punctuation">,</span></span>
<span class="line">  <span class="token literal-property property">payload</span><span class="token operator">:</span> <span class="token keyword">null</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token literal-property property">callback</span><span class="token operator">:</span> <span class="token keyword">null</span><span class="token punctuation">,</span></span>
<span class="line"></span>
<span class="line">  <span class="token literal-property property">next</span><span class="token operator">:</span> <span class="token keyword">null</span><span class="token punctuation">,</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,14)),n("blockquote",null,[n("p",null,[e[3]||(e[3]=n("code",null,"update",-1)),e[4]||(e[4]=a(" return by ",-1)),e[5]||(e[5]=n("code",null,"createUpdate",-1)),e[6]||(e[6]=a(", ",-1)),n("a",r,[e[0]||(e[0]=a("you can check the source code of ",-1)),e[1]||(e[1]=n("code",null,"createUpdate",-1)),e[2]||(e[2]=a(" here.",-1)),o(t)])])]),e[31]||(e[31]=s(`<ul><li>eventTime: time get by <code>performance.now()</code>. This will rebuild soon, so we do not need get deep into it.</li><li>lane: the <code>priority</code> of the tasks.</li><li>suspenseConfig: <code>Suspense</code> related.</li><li>tag: types of update, includings <code>UpdateState</code>, <code>ReplaceState</code>, <code>ForceUpdate</code> and <code>CaptureUpdate</code>.</li><li>payload: updated mount data, different type of components mount different data. As for <code>ClassComponent</code>, <code>payload</code> is the first parameter of <code>this.setState</code>. As for <code>HostRoot</code>, <code>payload</code> is the first parameter of <code>ReactDOM.render</code>.</li><li>callback: the callback fucntion of the update. This is the <code>callback fucntion</code> we have mentioned in the previous article <code>commit - layout</code>.</li><li>next: generate a chain with other <code>Update</code>.</li></ul><h2 id="connection-between-update-and-fiber" tabindex="-1"><a class="header-anchor" href="#connection-between-update-and-fiber"><span>Connection between Update and Fiber</span></a></h2><p>We find out there is a field <code>next</code> connect with other <code>Update</code> exsiting on the <code>Update</code>. Does it related with another <code>Fiber</code> in <code>React</code> ? The answer is yes.</p><p>From the previous section <code>Double Buffer</code>, we know that there are maximun of two <code>Fiber tree</code> existing in the page. One is the <code>current Fiber tree</code> and another is <code>workInProgress Fiber tree</code>. Just like how <code>Fiber Node</code> make up the <code>Fiber tree</code>, the multiple <code>Update</code> in the <code>Fiber treee</code> will be inclued in the <code>fiber.updateQueue</code>.</p><div class="custom-container tip"><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" viewBox="0 0 24 24"><g fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"></circle><path d="M12 8h.01"></path><path d="M11 12h1v4h1"></path></g></svg><p class="custom-container-title">TIP</p><p>You might think about why there could be multiple <code>Update</code> on a <code>Fiber node</code>.</p><p>Check this example</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line"><span class="token function">onClick</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">setState</span><span class="token punctuation">(</span><span class="token punctuation">{</span></span>
<span class="line">    <span class="token literal-property property">a</span><span class="token operator">:</span> <span class="token number">1</span></span>
<span class="line">  <span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">  <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">setState</span><span class="token punctuation">(</span><span class="token punctuation">{</span></span>
<span class="line">    <span class="token literal-property property">b</span><span class="token operator">:</span> <span class="token number">2</span></span>
<span class="line">  <span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>this.onClick</code> triggers in the first <code>ClassComponent</code> and <code>this.setState</code> called twice inside the funtion which means there are two <code>Update</code> happened in the <code>fiber</code>.</p></div><p>There could be maximum two <code>updateQueue</code> exsiting on the <code>Fiber node</code>. Which are the <code>current updateQueue</code> saved by the <code>current fiber</code> and the <code>workInProgress updateQueue</code> which saved by the <code>workInProgress fiber</code>.</p><h1 id="updatequeue" tabindex="-1"><a class="header-anchor" href="#updatequeue"><span>updateQueue</span></a></h1><p>There are three types of <code>updateQueue</code>, we have talked about the type used for <code>HostComponent</code> in thr previous <code>completeWork</code> article.</p><p>Other two types are corresponding of the <code>Update</code> types.</p><p>The structure of <code>UpdateQueue</code> for <code>ClassComponent</code> and <code>HostRoot</code> looks like</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line"><span class="token keyword">const</span> <span class="token literal-property property">queue</span><span class="token operator">:</span> UpdateQueue<span class="token operator">&lt;</span>State<span class="token operator">&gt;</span> <span class="token operator">=</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token literal-property property">baseState</span><span class="token operator">:</span> fiber<span class="token punctuation">.</span>memoizedState<span class="token punctuation">,</span></span>
<span class="line">  <span class="token literal-property property">firstBaseUpdate</span><span class="token operator">:</span> <span class="token keyword">null</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token literal-property property">lastBaseUpdate</span><span class="token operator">:</span> <span class="token keyword">null</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token literal-property property">shared</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token literal-property property">pending</span><span class="token operator">:</span> <span class="token keyword">null</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token punctuation">}</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token literal-property property">effects</span><span class="token operator">:</span> <span class="token keyword">null</span><span class="token punctuation">,</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,11)),n("blockquote",null,[n("p",null,[e[10]||(e[10]=n("code",null,"UpdateQueue",-1)),e[11]||(e[11]=a(" return by ",-1)),e[12]||(e[12]=n("code",null,"initializeUpdateQueue",-1)),e[13]||(e[13]=a(", ",-1)),n("a",u,[e[7]||(e[7]=a("You can check the source code of ",-1)),e[8]||(e[8]=n("code",null,"initializeUpdateQueue",-1)),e[9]||(e[9]=a(" from here.",-1)),o(t)])])]),e[32]||(e[32]=s(`<ul><li><p>baseState: the <code>state</code> of the before updated <code>Fiber node</code>, and <code>Update</code> calculate the new <code>state</code> based on this.</p><blockquote><p>It looks like the <code>master</code> branch from the mental model.</p></blockquote></li><li><p><code>firstBaseUpdate</code> and <code>lastBaseUpdate</code>: the <code>Update</code> saved before the <code>Fiber node</code> updated. It saved as a chain with the chain header named <code>firstBaseUpdate</code> and the end of the chian named <code>lastBaseUpdate</code>.</p><p>The reason why we got some <code>Update</code> in the unupdated <code>Fiber node</code>, is because some of the <code>low priority Update</code> was skipped in the last time update.</p><blockquote><p><code>baseUpdate</code> like the <code>commit</code> of the <code>git rebase</code> from the mental model.</p></blockquote></li><li><p>shared.pending: When trigger the updates, <code>Update</code> will save in the <code>shared.pending</code> as an one-way cyclic chain table. This chain will be clipped and connected after <code>lastBaseUpdate</code> when the <code>Update</code> start caculating <code>state</code>.</p></li><li><p>effects: save <code>Update</code> which is <code>update.callback !== null</code>.</p></li></ul><h2 id="demo" tabindex="-1"><a class="header-anchor" href="#demo"><span>Demo</span></a></h2><p>For example, there is a <code>fiber</code> just finshed <code>commit stage</code> and rendered.</p><p>There are two <code>Update</code> did not processed in the last <code>render</code> because of the <code>low priority</code>, they will be the <code>baseUpdate</code> of next turn updating.</p><p>We name these <code>u1</code> and <code>u2</code>, which <code>u1.next === u2</code>.</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line">fiber<span class="token punctuation">.</span>updateQueue<span class="token punctuation">.</span>firstBaseUpdate <span class="token operator">===</span> u1</span>
<span class="line">fiber<span class="token punctuation">.</span>updateQueue<span class="token punctuation">.</span>lastBaseUpdate <span class="token operator">===</span> u2</span>
<span class="line">u1<span class="token punctuation">.</span>next <span class="token operator">===</span> u2</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>If we use <code>--&gt;</code> presents the direction of the chain.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">fiber.updateQueue.baseUpdate: u1 --&gt; u2</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>If we trigger update twice on <code>fiber</code>, there will be two new <code>Update</code> created, we call them <code>u3</code> and <code>u4</code>.</p><p>Each <code>update</code> will insert to the <code>updateQueue</code> queue by <code>enqueneUpdate</code> method.</p><p>After inserting <code>u3</code></p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line">fiber<span class="token punctuation">.</span>updateQueue<span class="token punctuation">.</span>shared<span class="token punctuation">.</span>pending <span class="token operator">===</span> u3</span>
<span class="line">u3<span class="token punctuation">.</span>next <span class="token operator">===</span> u3</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>the circle chain relationship of <code>shared,pending</code> looks like</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">fiber.updateQueue.shared.pending:   u3 ─────┐</span>
<span class="line">                                     ^      |</span>
<span class="line">                                     └──────┘</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>after insert <code>u4</code></p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line">fiber<span class="token punctuation">.</span>updateQueue<span class="token punctuation">.</span>shared<span class="token punctuation">.</span>pending <span class="token operator">===</span> u4</span>
<span class="line">u4<span class="token punctuation">.</span>next <span class="token operator">===</span> u3</span>
<span class="line">u3<span class="token punctuation">.</span>next <span class="token operator">===</span> u4</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>the circle chain look like</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">fiber.updateQueue.shared.pending:   u4 ──&gt; u3</span>
<span class="line">                                     ^      |</span>
<span class="line">                                     └──────┘</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,18)),n("p",null,[e[17]||(e[17]=n("code",null,"shared.pending",-1)),e[18]||(e[18]=a(" will promise that it points to the last insert ",-1)),e[19]||(e[19]=n("code",null,"update",-1)),e[20]||(e[20]=a(", ",-1)),n("a",h,[e[14]||(e[14]=a("You can check the source code of ",-1)),e[15]||(e[15]=n("code",null,"enqueueUpdate",-1)),e[16]||(e[16]=a(" here.",-1)),o(t)])]),e[33]||(e[33]=s(`<p>After update, it will goes to <code>render</code>.</p><p>Right now, the circle chain of <code>shared.pending</code> is clipped and joined behind the <code>updateQueue.lastBaseUpdate</code>.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">fiber.updateQueue.baseUpdate: u1 --&gt; u2 --&gt; u3 --&gt; u4</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Following by traverse the <code>updateQueue.baseUpdate</code> chain table. Use <code>fiber,updateQueue.baseState</code> as the <code>initialization state</code>, traversal and caculate the the <code>state</code> created by each <code>Update</code>.</p><p>The <code>Update</code> with <code>low priority</code> will be skipped when traversalling.</p>`,5)),n("blockquote",null,[n("p",null,[e[24]||(e[24]=n("code",null,"update",-1)),e[25]||(e[25]=a(" of the ",-1)),e[26]||(e[26]=n("code",null,"render",-1)),e[27]||(e[27]=a(" finished by ",-1)),e[28]||(e[28]=n("code",null,"processUpdaetQueue",-1)),e[29]||(e[29]=a(", ",-1)),n("a",b,[e[21]||(e[21]=a("you can check the source code of the ",-1)),e[22]||(e[22]=n("code",null,"processUpdateQueue",-1)),e[23]||(e[23]=a(" from here.",-1)),o(t)])])]),e[34]||(e[34]=s("<p>The changes of the <code>state</code> will have different <code>JSX object</code> when it rendering, have <code>effectTag</code> by <code>Diff algorithem</code> and render on the page during <code>commit</code>.</p><p>After rendering, the <code>workInProgress Fiber tree</code> becomes <code>current Fiber tree</code> and that is the end of this updating process.</p>",2))])}const f=d(c,[["render",m]]),g=JSON.parse('{"path":"/posts/reactSourceCode/state/update.html","title":"State -- Update","lang":"en-US","frontmatter":{"title":"State -- Update","date":"2023-06-15","categories":["React","State"]},"headers":[{"level":2,"title":"Foreword","slug":"foreword","link":"#foreword","children":[]},{"level":2,"title":"Type of Update","slug":"type-of-update","link":"#type-of-update","children":[]},{"level":2,"title":"Structure of Update","slug":"structure-of-update","link":"#structure-of-update","children":[]},{"level":2,"title":"Connection between Update and Fiber","slug":"connection-between-update-and-fiber","link":"#connection-between-update-and-fiber","children":[]},{"level":2,"title":"Demo","slug":"demo","link":"#demo","children":[]}],"git":{"createdTime":1785043051000,"updatedTime":1785144585000,"contributors":[{"name":"harry","email":"hanyao@compac.co.nz","commits":2}]},"filePathRelative":"posts/reactSourceCode/state/update.md"}');export{f as comp,g as data};
