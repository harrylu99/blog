import{_ as o,c as t,b as c,o as d}from"./app-B8SEAMDd.js";const a={};function r(n,e){return d(),t("div",null,[...e[0]||(e[0]=[c('<p>Before we jump into the source code, we should start from the <code>mental model</code> of the <code>updating</code>.</p><h2 id="synchronous-update-of-react" tabindex="-1"><a class="header-anchor" href="#synchronous-update-of-react"><span>Synchronous Update of React</span></a></h2><p>We could understanding <code>update</code> as the <code>code version control</code>.</p><p>Before we have <code>code version control</code>, we added the functions one by one into our code, everything looks perfect untill we meet a <code>bug</code>.</p><p>For solving this, we have to push our previous changes before we start fix it.</p><p>In <code>React</code>, the apps created by <code>ReactDOM.render</code> are using this <code>updating</code> rules.</p><p>Which means, there is not <code>priority</code> in the <code>updating</code>, even the <code>bug</code> with <code>high priority</code> needs to fix after other <code>update</code>.</p><h2 id="concurrent-update" tabindex="-1"><a class="header-anchor" href="#concurrent-update"><span>Concurrent Update</span></a></h2><p>When we got the <code>code version control</code>, we could temporary <code>stash</code> the current changes before we fix the <code>bug</code>.</p><p>After the <code>bug</code> fixed, we could use <code>git rebase</code> rebase with the <code>develop branch</code>, then you can keep going with the tasks.</p><p>In <code>React</code>, ths apps created by <code>ReactDOM.createBlockingRoot</code> or <code>ReactDOM.createRoot</code> are using <code>concurrent update</code>.</p><p><code>high priority update</code> will stop the <code>low priority update</code>, which means it needs to finished the whole process from <code>render</code> to <code>commit</code>, after that, those <code>low priority</code> updates are going to <code>reupdate</code> based on the result of the <code>high priority update</code>.</p><p>We are going to talk about the implement of the <code>concurrent update</code> in the next aritlce.</p>',13)])])}const i=o(a,[["render",r]]),p=JSON.parse('{"path":"/posts/reactSourceCode/state/mentalModel.html","title":"State -- Mental Model","lang":"en-US","frontmatter":{"title":"State -- Mental Model","date":"2023-06-15","categories":["React","State"]},"headers":[{"level":2,"title":"Synchronous Update of React","slug":"synchronous-update-of-react","link":"#synchronous-update-of-react","children":[]},{"level":2,"title":"Concurrent Update","slug":"concurrent-update","link":"#concurrent-update","children":[]}],"git":{"createdTime":1785043051000,"updatedTime":1785144585000,"contributors":[{"name":"harry","email":"hanyao@compac.co.nz","commits":2}]},"filePathRelative":"posts/reactSourceCode/state/mentalModel.md"}');export{i as comp,p as data};
