import{_ as c,c as p,a as n,d as s,e as t,b as o,r as i,o as l}from"./app-B8SEAMDd.js";const r={},d={href:"https://github.com/sebmarkbage/",target:"_blank",rel:"noopener noreferrer"},u={href:"https://codesandbox.io/s/frosty-hermann-bztrp?file=/src/index.js:152-160",target:"_blank",rel:"noopener noreferrer"};function k(h,e){const a=i("ExternalLinkIcon");return l(),p("div",null,[e[6]||(e[6]=n("h2",{id:"foreword",tabindex:"-1"},[n("a",{class:"header-anchor",href:"#foreword"},[n("span",null,"Foreword")])],-1)),n("p",null,[e[1]||(e[1]=s("React core team member ",-1)),n("a",d,[e[0]||(e[0]=s("Sebastian Markbåge",-1)),t(a)]),e[2]||(e[2]=s(" once said: We React what is done in pratice is ",-1)),e[3]||(e[3]=n("code",null,"Algebraic Effects",-1)),e[4]||(e[4]=s(".",-1))]),e[7]||(e[7]=o(`<p>So, what is <code>Algebraic Effects</code> and how does it related with <code>React</code>?</p><h2 id="algebraic-effects" tabindex="-1"><a class="header-anchor" href="#algebraic-effects"><span>Algebraic Effects</span></a></h2><p><code>Algebraic Effects</code> is a concept of the <code>functional programming</code>, it could be used for separating <code>side effects</code> from the <code>functions</code>.</p><p>We are going to use fake syntax for explain it.</p><p>Think about if we have a function <code>getTotalPicNum</code> and after receive two <code>user</code>, it could search for the number of pics in the platform from both users and return a total pic number.</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line"><span class="token keyword">function</span> <span class="token function">getTotalPicNum</span><span class="token punctuation">(</span><span class="token parameter">user1<span class="token punctuation">,</span> user2</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token keyword">const</span> picNum1 <span class="token operator">=</span> <span class="token function">getPicNum</span><span class="token punctuation">(</span>user1<span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">const</span> picNum2 <span class="token operator">=</span> <span class="token function">getPicNum</span><span class="token punctuation">(</span>user2<span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">  <span class="token keyword">return</span> picNum1 <span class="token operator">+</span> picNum2</span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>We do not need to focus on the implement of <code>getPicNum</code> in the above function. What we do care is the process how it return the total of these two numbers.</p><p>Next, we trying to implement the <code>getPicNum</code>.</p><p>The number of pics is stored in the server so that we need to do an asynchronous request. And I think we could use <code>async await</code> for handle the request.</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line"><span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">getTotalPicNum</span><span class="token punctuation">(</span><span class="token parameter">user1<span class="token punctuation">,</span> user2</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token keyword">const</span> picNum1 <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">getPicNum</span><span class="token punctuation">(</span>user1<span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">const</span> picNum2 <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">getPicNum</span><span class="token punctuation">(</span>user2<span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">  <span class="token keyword">return</span> picNum1 <span class="token operator">+</span> picNum2</span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>However, the functions which call the <code>async</code> function should be <code>async</code> as well, which changed the synchronous of <code>getTotalPicNum</code>.</p><p>Any solutions for us to solve this problem? Unfortunally, there is no way for implement asynchronous requests when keep use the current <code>getTotalPicNum</code>.</p><p>We will create a fake syntax for solving it. Let&#39;s create a syntax that is similar with <code>try...catch</code>. We could named it <code>try...handle</code> with two opeartors <code>perform</code> and <code>resume</code>.</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line"><span class="token keyword">function</span> <span class="token function">getPicNum</span><span class="token punctuation">(</span><span class="token parameter">name</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token keyword">const</span> picNum <span class="token operator">=</span> perform name<span class="token punctuation">;</span></span>
<span class="line">  <span class="token keyword">return</span> picNum<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">try</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token function">getTotalPicNum</span><span class="token punctuation">(</span><span class="token string">&#39;Evan&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;You&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span> <span class="token function">handle</span> <span class="token punctuation">(</span><span class="token parameter">who</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token keyword">switch</span> <span class="token punctuation">(</span>who<span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">case</span> <span class="token string">&#39;Evan&#39;</span><span class="token operator">:</span></span>
<span class="line">      resume <span class="token keyword">with</span> <span class="token number">120</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token keyword">case</span> <span class="token string">&#39;You&#39;</span><span class="token operator">:</span></span>
<span class="line">      resume <span class="token keyword">with</span> <span class="token number">240</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token keyword">default</span><span class="token operator">:</span></span>
<span class="line">      resume <span class="token keyword">with</span> <span class="token number">0</span><span class="token punctuation">;</span></span>
<span class="line">  <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>When <code>getPicNum</code> have been called, <code>perform name</code> would be executed, followed by the function execution stack will pop out from <code>getPicNum</code> and be catched by the nearest <code>try...handle</code>.</p><p><code>Error</code> would be the parameter of <code>catch</code> after <code>throw Error</code> and <code>name</code> would be the parameter of <code>handle</code> after <code>perform name</code>.</p><p>After <code>Error</code> catched by the <code>catch</code>, the previous stack will be destroyed and back to the execution stack of <code>perform</code> after <code>handle</code> execute the <code>resume</code>.</p><p>For <code>case &#39;Evan&#39;</code>, the execution stack will back to <code>getPicNum</code> after executed <code>resume with 120</code>. <code>picNum === 120</code> in this time.</p><div class="custom-container tip"><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" viewBox="0 0 24 24"><g fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"></circle><path d="M12 8h.01"></path><path d="M11 12h1v4h1"></path></g></svg><p class="custom-container-title">Tip</p><p><code>try...catch</code> is a fake syntax for explaining the <code>Algebraic Effects</code>.</p></div><p>To sum up, <code>Algebraic Effects</code> could separate <code>side effects</code> which is getting the number of pics in the example from the function logic, help the focus point of the function stay pure. Furthemore, we could see that it does not need to care about synchronous or asynchronous in <code>perform resume</code>.</p><h2 id="algebraic-effects-in-react" tabindex="-1"><a class="header-anchor" href="#algebraic-effects-in-react"><span>Algebraic Effects in React</span></a></h2><p>The most common thing we could find that implement <code>Algebraic Effects</code> in <code>React</code> is <code>Hooks</code>.</p><p>When we using the <code>Hook</code> like <code>useState</code>, <code>useReducer</code> and <code>useRef</code>, we do not need to think about how does the <code>state</code> of <code>FunctionComponent</code> saved in <code>Hook</code> which <code>React</code> have already implement for us.</p><p>We only need to write the business logic when we using <code>useState</code>.</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line"><span class="token keyword">function</span> <span class="token function">App</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token keyword">const</span> <span class="token punctuation">[</span>num<span class="token punctuation">,</span> updateNum<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token function">useState</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">  <span class="token keyword">return</span> <span class="token operator">&lt;</span>button onClick<span class="token operator">=</span><span class="token punctuation">{</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token function">updateNum</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token parameter">num</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> num <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token operator">&gt;</span><span class="token punctuation">{</span>num<span class="token punctuation">}</span><span class="token operator">&lt;</span><span class="token operator">/</span>button<span class="token operator">&gt;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,25)),n("p",null,[n("a",u,[e[5]||(e[5]=s("Check this Suspense Demo from React team",-1)),t(a)])]),e[8]||(e[8]=o(`<p><code>ProfileDetails</code> for showing the User&#39;s name in the <code>Demo</code> and User&#39;s name is <code>asynchronous request</code>. However, <code>Demo</code> is synchronous.</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line"><span class="token keyword">function</span> <span class="token function">ProfileDetails</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token keyword">const</span> user <span class="token operator">=</span> resource<span class="token punctuation">.</span>user<span class="token punctuation">.</span><span class="token function">read</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">return</span> <span class="token operator">&lt;</span>h1<span class="token operator">&gt;</span><span class="token punctuation">{</span>user<span class="token punctuation">.</span>name<span class="token punctuation">}</span><span class="token operator">&lt;</span><span class="token operator">/</span>h1<span class="token operator">&gt;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="algebraic-effects-and-generator" tabindex="-1"><a class="header-anchor" href="#algebraic-effects-and-generator"><span>Algebraic Effects and Generator</span></a></h2><p>One of the purpose of re-building <code>Reconciler</code> in <code>React16</code> is to change the synchronous update to the asynchronous update which could be paused.</p><p>Which means, the updates could be paused or break from the middle of the tasks and it could keep handling with the previous task after resume.</p><p>And that is the effect from the <code>Algebraic Effects</code>.</p><p><code>Generator</code> is very similar to this and it has been built in the browser. However, <code>React</code> team gave up for using it based on some disadvantages from the <code>Generator</code>.</p><ul><li><code>Generators</code> does not just let you yield in the middle of a stack. You have to wrap every single function in a <code>generator</code>. This not only adds a lot of syntactic overhead but also runtime overhead in any existing implementation. It&#39;s fair that the syntax might be more helpful than not, but the perf issue still stands.</li><li>The biggest reason, however, is that <code>generators</code> are stateful. You can not resume in the middle of it.</li></ul><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre><code><span class="line"><span class="token keyword">function</span><span class="token operator">*</span> <span class="token function">doWork</span><span class="token punctuation">(</span><span class="token parameter"><span class="token constant">A</span><span class="token punctuation">,</span> <span class="token constant">B</span><span class="token punctuation">,</span> <span class="token constant">C</span></span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token keyword">var</span> x <span class="token operator">=</span> <span class="token function">doExpensiveWorkA</span><span class="token punctuation">(</span><span class="token constant">A</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">yield</span></span>
<span class="line">  <span class="token keyword">var</span> y <span class="token operator">=</span> x <span class="token operator">+</span> <span class="token function">doExpensiveWorkB</span><span class="token punctuation">(</span><span class="token constant">B</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">yield</span></span>
<span class="line">  <span class="token keyword">var</span> z <span class="token operator">=</span> y <span class="token operator">+</span> <span class="token function">doExpensiveWorkC</span><span class="token punctuation">(</span><span class="token constant">C</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">return</span> z</span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Everytime when the browser has free time, it executes one of the <code>doExpensiveWorks</code> in turn and will be paused when the time runs out, then resumes from where it left off when it resumes again.</p><p>That could be a good case for using <code>Generator</code> when we only need to consider about one task pause or resume.</p><p>Besides, when we thinking about when the high priority task is cutting the line after <code>doExpensiveWorkA</code> and <code>doExpensiveWorkB</code> have finished their job. At this moment, component <code>B</code> has recieve a <code>priority update</code>, however, since the <code>generators</code> are stateful, the value <code>x</code> and <code>y</code> cannot be reuse here, which means it has to be re-caculate.</p><p>Based on that, <code>React</code> did not choose use <code>Generator</code> for implement the <code>Reconciler</code>.</p><h2 id="algebraic-effects-and-fiber" tabindex="-1"><a class="header-anchor" href="#algebraic-effects-and-fiber"><span>Algebraic Effects and Fiber</span></a></h2><p><code>Fiber</code>, as the same as <code>Process</code>, <code>Thread</code> and <code>Coroutine</code>, it is one of the virtual concepts of the operating system. Most articles think <code>Fiber</code> is an implementation of the <code>Coroutine</code>. In JavaScript, the implementation of <code>Coroutine</code> is <code>Generator</code>.</p><p>Now, we could think both of <code>Fiber</code> and <code>Generator</code> as an implemetation of the Algebraic Effects in JS.</p><p><code>React</code> has its own state update mechanism, which support task with different priority, could pause and resume and could resumes from where it left off. In fact, each updated task unit is the <code>Fiber</code> of the <code>React Element</code>.</p><p>We will talk about how <code>Fiber</code> implement in the next section.</p>`,18))])}const f=c(r,[["render",k]]),b=JSON.parse('{"path":"/posts/reactSourceCode/fiber/mentalModel.html","title":"Fiber -- Mental Model","lang":"en-US","frontmatter":{"title":"Fiber -- Mental Model","date":"2023-02-15","categories":["React","Fiber Architecture"]},"headers":[{"level":2,"title":"Foreword","slug":"foreword","link":"#foreword","children":[]},{"level":2,"title":"Algebraic Effects","slug":"algebraic-effects","link":"#algebraic-effects","children":[]},{"level":2,"title":"Algebraic Effects in React","slug":"algebraic-effects-in-react","link":"#algebraic-effects-in-react","children":[]},{"level":2,"title":"Algebraic Effects and Generator","slug":"algebraic-effects-and-generator","link":"#algebraic-effects-and-generator","children":[]},{"level":2,"title":"Algebraic Effects and Fiber","slug":"algebraic-effects-and-fiber","link":"#algebraic-effects-and-fiber","children":[]}],"git":{"createdTime":1785043051000,"updatedTime":1785144585000,"contributors":[{"name":"harry","email":"hanyao@compac.co.nz","commits":2}]},"filePathRelative":"posts/reactSourceCode/fiber/mentalModel.md"}');export{f as comp,b as data};
